<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/osmedium</item>
    </style>

    <style name="MyToolbarTheme" parent="@style/ThemeOverlay.AppCompat.Dark">
        <item name="colorControlNormal">@color/whiteSmoke</item>
        <item name="titleTextColor">@color/whiteSmoke</item>
        <item name="android:textSize">17sp</item>
    </style>

    <style name="ItemTheme" parent="CardView">
        <item name="android:layout_margin">@dimen/item_spacing_half</item>
        <item name="android:clickable">true</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
        <item name="cardElevation">@dimen/margin_extra_tiny</item>
    </style>

    <style name="LeagueItemText">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:ellipsize">end</item>
        <item name="android:singleLine">true</item>
        <item name="android:lineSpacingMultiplier">1.1</item>
        <item name="android:lines">1</item>
    </style>

    <style name="BookmarkButton" parent="Widget.AppCompat.Light.ActionButton">
        <item name="android:layout_height">@dimen/abc_action_button_min_height_material</item>
        <item name="android:layout_width">@dimen/abc_action_button_min_height_material</item>
        <item name="android:src">@drawable/ic_bookmark_border</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
